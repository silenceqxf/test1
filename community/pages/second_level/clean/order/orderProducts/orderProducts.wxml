<!--pages/second_level/clean/order/orderProducts/orderProducts.wxml-->
<view>
  <block wx:for="{{products}}" wx:for-item="product" wx:key="p_id">
    <view class="field_item vc" data-id="{{product.p_id}}" bindtap="itemClick">{{product.p_title}}</view>
  </block>
</view>
